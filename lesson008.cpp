#include <iostream>

using namespace std;
/*
int main() {
	//int* ptr = static_cast<int*>(malloc(sizeof(int)));
	int* ptr = static_cast<int*>(calloc(1, sizeof(int)));
	*ptr = 7;
	cout << *ptr << endl;
	free(ptr);


 	return 0;
 }
 */

