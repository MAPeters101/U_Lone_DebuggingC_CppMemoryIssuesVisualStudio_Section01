#include <iostream>

using namespace std;

/*
int main() {
	int* ptr = static_cast<int*>(calloc(1, sizeof(int)));
	*ptr = 7;
	cout << *ptr << endl;
	cout << "Initial mem size: " << _msize(ptr) << endl;
	//ptr = static_cast<int*>(realloc(ptr, sizeof(int) * UINT32_MAX));
	if (ptr == NULL) {
		cout << "Could not allocate memory" << endl;
		return -1;
	}
	cout << "After mem size: " << _msize(ptr) << endl;

	free(ptr);

	return 0;
}
*/
